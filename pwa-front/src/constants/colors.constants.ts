type ColorsType = {
  primary: string;
  secondary: string;
  white: string;
  error: string;
  secondaryText: string;
};

const Colors: ColorsType = {
  primary: '#fff',
  secondary: '#fff',
  white: '#fff',
  error: '#fff',
  secondaryText: '#fff',
};

export default Colors;
